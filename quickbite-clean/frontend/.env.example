# Backend API base URL used at build time by the frontend.
# Local development can continue using the package.json proxy with /api.
# Production example: https://your-backend-domain.com/api
REACT_APP_API_URL=/api
